// Meal Database Row Entity Schema
export interface Food {
  id: number;
  name: string;
  // image?: string;
  // portion: number;
  // kcal: number;
  // carbs: number;
  // protein: number;
  // fat: number;
  // fiber: number;
  // price: number;
}
